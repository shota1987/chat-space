.main
  .main__main-header
    .main__main-header__left-box
      %h2.main__main-header__left-box__current-group
        = @group.name
        %ul.main__main-header__left-box__member-list
          Member：
          %li.main__main-header__left-box__member-list__member
            - @group.group_users.each do |group_user|
              = group_user.user.name
    = link_to edit_group_path(@group) do
      .main__main-header__edit-btn
        Edit

  .main__contents
    = render @messages
  
  .form
    = form_for [@group, @message] do |f|
      .form__mask
        = f.text_field :content, class: 'form__mask__message', placeholder: 'type a message'
        = f.label :image, class: 'form__mask__image' do
          = icon('fas', 'image', class: 'icon')
          = f.file_field :image, class: 'hidden'
      .form__send
        = f.submit 'Send', class: 'form__send__submit'
